// LOCAL EMULATOR RULES ONLY.
// Intentionally permissive for the Storage Emulator. Not the production
// security rules and must never be deployed against a real project.
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
