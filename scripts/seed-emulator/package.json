{
  "name": "oesperformance-seed-emulator",
  "private": true,
  "version": "1.0.0",
  "description": "One-off dev utility: seeds the local Firebase Emulator Suite with sample data for OESPerformance. Not part of the deployed app — scoped to this folder so it never affects the static site build/deploy.",
  "type": "commonjs",
  "scripts": {
    "seed": "node seed-emulator.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  }
}
